<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mandarinapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".PhrasesActivity"
            android:label="@string/string_pharses"></activity>
        <activity
            android:name=".ColorsActivity"
            android:label="@string/string_colors" />
        <activity
            android:name=".FamilyMembersActivity"
            android:label="@string/string_family" />
        <activity
            android:name=".NumbersActivity"
            android:label="@string/string_numbers" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>